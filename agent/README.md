# Agent

Sends heartbeat + system metrics to the server.

## Run
1) Copy config:
- copy config.example.json to config.json

2) Install deps:
- pip install -r requirements.txt

3) Start:
- py -3.10 .\agent.py
